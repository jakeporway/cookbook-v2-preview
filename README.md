# cookbook-v2-preview (redirect only)

The Nonprofit AI Cookbook V2 moved to **https://jakeporway.github.io/cookbook-v2/**
(repo: `jakeporway/cookbook-v2`).

This repo exists only so previously shared `/cookbook-v2-preview/` links keep
working. `index.html` redirects to the new home; `404.html` forwards deep links
to the matching page there. There is no site content here — do not add any.
